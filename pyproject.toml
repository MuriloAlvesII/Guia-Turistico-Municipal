[project]
name = "guia-turistico-municipal"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [fastapi, FastAPI, HTTPException, sqlite3, pydantic, BaseModel , EmailStr, typing, List, uuid]
